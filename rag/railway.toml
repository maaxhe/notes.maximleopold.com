[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npm run rag:server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[health]
path = "/health"
port = 3030
